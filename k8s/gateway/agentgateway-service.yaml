apiVersion: v1
kind: Service
metadata:
  name: agentgateway
  labels:
    app: agentgateway
spec:
  selector:
    app: agentgateway
  ports:
    - name: llm
      port: 3000
      targetPort: 3000
    - name: mcp
      port: 3001
      targetPort: 3001
